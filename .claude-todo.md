# libreFS-cli pending tasks

## Module path rename
Update go.mod module path from `github.com/minio/mc` → `github.com/libreFS/librefs-cli`.
Requires: updating all internal imports, console/go.mod, server/go.mod indirect dep.
Same work as the server rename — defer until a dedicated refactor window.
Tracked here until we open a GitHub issue for it.
